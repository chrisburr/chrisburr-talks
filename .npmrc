# Supply-chain cooldown: never install a version published less than 7 days
# ago (npm's native min-release-age, requires npm >= 11.10.0 — bundled with the
# node pinned in pixi.lock). Catches transitive deps too, and gates anything
# Renovate regenerates via npm. Do not combine with npm's --before flag.
min-release-age=7
