# Build artifacts (regenerated by `pixi run build` / `pixi run index`)
# Decks build to <deck>/index.html; the root index.html is the landing page.
**/index.html
**/slides.pdf
/_site/

# Environments & dependencies
/.pixi/
/node_modules/

# OS / editor cruft
.DS_Store
